doctype html
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1= pageName

      p Add your new password
      form.caja-login(method="POST")
        .campo
          label(for="password") Password:
          input(type="password" name="password" placerholder="New Password")
        .campo.enviar
          input.boton(type="submit" value="Reset password")

    // pre= locals.vardump(locals.messages)
    // pre= locals.vardump(messages)